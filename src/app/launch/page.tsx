import { LaunchControllerApp } from "@/components/LaunchControllerApp";

export const metadata = {
  title: "SD Launch Controller"
};

export default function LaunchPage() {
  return <LaunchControllerApp />;
}
